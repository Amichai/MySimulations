<UserControl x:Class="ChartingHelper.Histogram"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock Margin="5">Bin Size:</TextBlock>
                <TextBox  Name="binSize" Margin="5" Width="100" Text="{Binding BinSize}" TextChanged="binSize_TextChanged"></TextBox>
                <Button Margin="5" Width="80" Click="redraw_Click">redraw</Button>
            </StackPanel>
        <oxy:Plot Name="Root" Model="{Binding Plot}">
        </oxy:Plot>
        </DockPanel>
    </Grid>
</UserControl>
